#!/bin/sh

if [ "${SYNOPKG_PKG_STATUS:-}" = "INSTALL" ]; then
    if ! printf '%s' "${wizard_token:-}" | grep -Eq '^[0-9a-fA-F]{64}$'; then
        echo "Omnisave requires a 64-character hexadecimal API token." > "${SYNOPKG_TEMP_LOGFILE}"
        exit 1
    fi
fi

exit 0
